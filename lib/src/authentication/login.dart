bool authenticateUser(String email, String password){
  // Replace with actual authentication user using supabase Authentication
  
  if (email=="kem" && password == "123" ){ return true;}
  return false;
}