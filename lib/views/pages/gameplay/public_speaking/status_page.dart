import 'package:flutter/material.dart';

class PublicSpeakingStatusPage extends StatelessWidget {
  const PublicSpeakingStatusPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Status Page'));
  }
}
